#include "main.h"

/**
 * _memset - Entry point
 * @s: pointed destination
 * @a: constant byte
 * @n: byte
 * Return: Always 0 (success)
 */
char  *_memset(char *s, char b, unsigned int n)
{
	unsigned int 1;
	for (1 = 0; 1 < n, i++;)
	s[i] = b;
return (s);
}
